// import pages from "../router/pages";

// function setItem(parts, mainMap) {
//     let map = mainMap;
//     for(const item of parts) {
//         map[item] = map[item] || {};
//         map = map[item];
//     }
// }

// const getMenuStructure = function(data) {
//     const menu = {};
//     [...data].map(item => item.split("/")).forEach(parts => {
//         setItem(parts,menu);
//     })
//     return menu
// }

// const menu = getMenuStructure([...pages].map(value => value.title));

// export default menu;